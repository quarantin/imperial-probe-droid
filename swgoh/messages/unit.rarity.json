{
    "author": {
        "name": "${nick}",
        "icon_url": "${user.avatar}"
    },
    "thumbnail": {
        "url": "${server}/avatar/${unit.id}?rarity=${rarity}"
    },
    "title": "${unit}",
    "description": "${stars}"
}
